name: feature_activity
description: Activity feed — chronological list of all transactions and events.
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../../core
  domain:
    path: ../../domain
  design_system:
    path: ../../design_system
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  data:
    path: ../../data

dev_dependencies:
  flutter_test:
    sdk: flutter
