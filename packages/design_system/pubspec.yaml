name: design_system
description: Splitbo design tokens, theme, and component library.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  # No Firebase. No Riverpod. No business logic. Zero.
  core:
    path: ../core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  golden_toolkit: ^0.15.0    # [VERIFY-BEFORE-USE] — check pub.dev for latest
  mocktail: ^1.0.4
